class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        // if all elements are odd or even then nothingto think ,
        // but when there are both odd and even elements present we can convert any even to odd by just substracting
        // even - odd = odd , whereeas odd - even = odd 
    
        return true;
    }
};